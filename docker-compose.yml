services:
  api-monitor:
    image: ${API_MONITOR_IMAGE:-ghcr.io/andlu155/api-monitor-worker:latest}
    container_name: api-monitor
    ports:
      - "3060:3000"
    env_file:
      - .env
    volumes:
      - api-monitor-data:/app/data
    restart: unless-stopped

volumes:
  api-monitor-data:
